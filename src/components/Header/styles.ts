import { styled } from "../../styles";

export const Container = styled("header", {
  background: "$gray-700",
  height: "12.5rem",
  marginTop: 0,
  width: "100%",
  display: "grid",
  placeContent: "center",
});
